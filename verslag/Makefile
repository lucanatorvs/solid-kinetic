verslag: main.tex
	pdflatex main
	pdflatex main
	mv main.pdf verslag.pdf
	-rm -f *.acn
	-rm -f *.aux
	-rm -f *.bbl
	-rm -f *.blg
	-rm -f *.glo
	-rm -f *.hst
	-rm -f *.idx
	-rm -f *.ist
	-rm -f *.lof
	-rm -f *.log
	-rm -f *.toc
	-rm -f *.ver
	-rm -f Sources/*.aux
